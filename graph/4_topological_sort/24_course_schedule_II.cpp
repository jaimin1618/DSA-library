#include<bits/stdc++.h>
using namespace std;

// Link: https://leetcode.com/problems/course-schedule-ii/description/
 
// Approach: Implement topological sort with DFS or BFS
// with DFS simpler DFS algorithm and push nodes (after neighbours visited) into the the Stack / deque or vector
// with BFS, apply kahn's algorithm
// Important point - check for CYCLE in both methods of topo sort. 

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	

	return 0;
}